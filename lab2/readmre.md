## fs module directly communicate with system rathher than browser the common operations on a file or folder :
1. readfile, writefile, appendfile
2. Folder
- mkdir, rdir, readdir
3. File Metaadta

4. Watch  -> watch , unwatch
5. Stream 





all function are promised so it be called with await